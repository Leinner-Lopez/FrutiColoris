<!doctype html>
<html lang="es">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/jpeg" href="/favicon.jpg" />
        <meta name="generator" content={Astro.generator} />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,500;0,600;1,500;1,600&display=swap"
            rel="stylesheet"
        />
        <title>Fruty Coloriss</title>
    </head>
    <body>
        <slot />
    </body>
</html>

<style is:global>
    :root {
        --color-bg: #f3e8d8;
        --color-surface: #ffffff;
        --color-border: #ece4d8;
        --color-ink: #2b241f;
        --color-muted: #8a8073;

        --color-pink: #ff1493;
        --color-pink-soft: #fdeaf3;
        --color-pink-ink: #b3106a;

        --color-blue: #0080d0;
        --color-blue-soft: #e6f4fc;
        --color-blue-ink: #0a6aa8;

        --color-orange: #ff6b35;
        --color-orange-soft: #fff0e8;
        --color-orange-ink: #c24e1f;

        --font-display: "Playfair Display", "DMSans", serif;
        --font-body:
            "DMSans", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
        font-family: var(--font-body);
        scroll-behavior: smooth;
    }

    html,
    body {
        display: flex;
        flex-direction: column;
        align-items: center;
        margin: 0;
        width: 100%;
        height: 100%;
        background-color: var(--color-bg);
        color: var(--color-ink);
    }
</style>
